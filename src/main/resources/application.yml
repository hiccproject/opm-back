spring:
  application:
    name: opm
  profiles:
    active: local
  logging:
    level:
      org:
        springframework:
          security: DEBUG
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB